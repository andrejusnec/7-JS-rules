const wordList = ['Labas', 'Rytas','Ieva'];
/*
for (let w = 0; w < wordList.length; w++) {
    const word = wordList[w];
    let backward = '';

    for (let r = 0; r < word.length; r++) {
        const letter = word[r];
        backward = letter + backward;
    }

    console.log(backward);
}
*/
/*
Einame per zodziu sarasa
    Pasiimu vienu konkretu zodi
        pasiruosiu kintamuji najuma atvirkstiniam zodziui
            Einu per to zodzio raides
                pasiimu to zodzio raide
                    pasidedu is priekio atvirkstiniam zodziui
            atspausdinu atv zodi
*/
/* CHECK THIS!!!!!!!!!!!!!!!!!!!
for(let w = 0; w < wordList.length; w++) {
    const word = wordList[w];
    let backward = '';

    for(let r = 0; r < word.length; r++); {
        const letter = word[r];
        backward = letter + backward;
    }
    console.log(backward);
}*/



